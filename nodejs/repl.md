# REPL이란?
자바스크립트는 스크립트 언어이기 때문에 미리 컴파일을 하지 않아도 즉석에서 코드를 실행할 수 있다. Chrome 브라우저 콘솔 탭과 비슷한 콘솔을 **Node**에서도 제공하는데, 입력한 코드를 읽고(Read), 해석하고(Eval), 결과물을 반환하고(Print), 종료할 때까지 반복(Loop)한다고 해서 **REPL**(Read Eval Print Loop)이라고 부른다.